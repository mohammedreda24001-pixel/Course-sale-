# Course-sale — Waseet-native Unified Patch

هذا الإصدار يستبدل منطق الشحن القديم بنموذج طلب مبني للوسيط من البداية. لا توجد عملية تحويل لاحقة من طلب قديم إلى طلب Waseet؛ الطلب الجديد يُنشأ محلياً بعد التحقق من بيانات Waseet الرسمية، ثم يُرسل ويُتابع عبر الوسيط.

## قبل التطبيق

1. خذ نسخة احتياطية كاملة من قاعدة بيانات Supabase.
2. احتفظ بنسخة من المشروع الحالي وبيئة الإنتاج.
3. لا تشغّل كويريات المراحل القديمة بعد هذا الكويري؛ الملف `Supabase-Waseet-Native-Full.sql` هو الترحيل الموحّد.
4. لا ترسل الطلبات القديمة جماعياً بعد الترحيل. أي طلب لا يحتوي IDs رسمية مؤكدة للمحافظة والمنطقة وحجم الطرد سيصبح `manual_review`.

## ترتيب التطبيق

1. افتح Supabase SQL Editor.
2. شغّل ملف `Supabase-Waseet-Native-Full.sql` كاملاً كعملية واحدة.
   - للأنظمة التي طبقت الملف الموحّد قبل دعم V2.3، شغّل بعدها الملف الإضافي
     `Supabase-Waseet-V2.3-Rate-Limit.sql` مرة واحدة.
3. أضف متغيرات البيئة الموجودة في `.env.example` إلى بيئة الخادم/Vercel.
4. ثبّت الحزم وشغّل الفحص:

   ```bash
   npm ci
   npm run validate
   ```

5. ارفع المشروع إلى بيئة تجريبية أولاً.
6. سجّل الدخول كمدير، ثم حدّث قوائم الوسيط من صفحة الإعدادات/إضافة الطلب.
7. أنشئ طلباً تجريبياً وراجعه ثم أرسله، وحدّث حالته واطبع ملصقه.
8. راجع كل طلب قديم يحمل `manual_review` وحدد المحافظة والمنطقة والحجم يدوياً قبل الإرسال.

## متغيرات البيئة

- `NEXT_PUBLIC_SUPABASE_URL`: رابط مشروع Supabase.
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: المفتاح العام للمتصفح، مع بقاء الوصول الفعلي للبيانات عبر APIs الخادم.
- `SUPABASE_SERVICE_ROLE_KEY`: للخادم فقط، ولا يوضع في متغير يبدأ بـ`NEXT_PUBLIC_`.
- `JWT_SECRET`: قيمة عشوائية لا تقل عن 32 محرفاً في الإنتاج.
- `WASEET_API_BASE_URL`: افتراضياً `https://api.alwaseet-iq.net/v1/merchant`.
- `WASEET_USERNAME` و`WASEET_PASSWORD`: بيانات حساب التاجر في الوسيط، للخادم فقط.
- `WASEET_TOKEN_TTL_SECONDS`: مدة Cache داخلية للتوكن.
- `WASEET_REQUEST_TIMEOUT_MS`: مهلة طلب API.
- `WASEET_MIN_REQUEST_INTERVAL_MS`: فاصل مركزي بين طلبات الوسيط؛ القيمة المقترحة `1050`.
- `CRON_SECRET`: اختياري لحماية مزامنة مجدولة مستقبلاً.

## نموذج الطلب الجديد

الحقول التشغيلية للشحن هي:

- اسم المستلم.
- الهاتف الأساسي والثانوي.
- `waseet_city_id` واسم المحافظة وقت الإنشاء.
- `waseet_region_id` واسم المنطقة وقت الإنشاء.
- تفاصيل العنوان داخل المنطقة.
- أقرب نقطة دالة.
- `waseet_package_size_id` والاسم وقت الإنشاء.
- مبلغ التحصيل الكامل.
- عدد القطع.
- نوع البضاعة والملاحظات.
- حالة الطلب الداخلية، حالة مزامنة Waseet، وحالة الشحنة الحية كقيم منفصلة.

لا توجد حقول إنشاء جديدة لـ`basePrice` أو`deliveryFee`. الأجور التي يرجعها الوسيط تُخزّن كمعلومات مالية بعد إنشاء الشحنة ولا يخمنها النظام مسبقاً.

## منع التكرار وحالات عدم اليقين

- إنشاء الطلب المحلي وحجز كود الدورة يتمان داخل RPC ذرية واحدة.
- رقم الوصل وبصمة Payload يعيدان نفس الطلب عند تكرار نفس طلب الحفظ بدلاً من استهلاك كود جديد.
- قبل إرسال الشحنة تُحجز محاولة Dispatch داخل قاعدة البيانات.
- إذا انقطعت الاستجابة بعد احتمال إنشاء الشحنة، تصبح الحالة `needs_verification` ويُمنع إعادة Create.
- من إجراء المدير يمكن ربط الطلب برقم الشحنة الموجود في الوسيط، أو تأكيد أن الشحنة لم تُنشأ بعد فحص التطبيق للسماح بمحاولة جديدة.

## الحالات

- `internal_order_state`: حالة السجل داخل Course-sale، مثل `ready` و`archived`.
- `waseet_sync_state`: حالة الاتصال والمزامنة، مثل `pending`, `syncing`, `synced`, `failed`, `needs_verification`, `manual_review`.
- `waseet_status_id` و`waseet_status_text`: رحلة الشحنة كما أعادها الوسيط.

لا توجد قائمة حالات شحن محلية قابلة لتغيير رحلة الشحنة يدوياً.

## Metadata الرسمية

المحافظات والمناطق وأحجام الطرود والحالات تُجلب من الوسيط وتُخزن في جداول Cache محلية. لا يوجد تحويل تلقائي إلى بغداد، ولا اختيار لأول Region، ولا ID ثابت لحجم الطرد. عند فشل تحديث كامل، يحتفظ النظام بالـSnapshot السابق بدلاً من تعطيله جزئياً.

تحديث الـMetadata يُثبت داخل معاملة قاعدة بيانات واحدة، وحد Waseet V2.3 البالغ 30 طلباً لكل 30 ثانية يُحجز مركزياً في Supabase حتى عند تشغيل أكثر من نسخة Serverless على Vercel.

## تعديل الطلب المرسل

عندما يحتوي الطلب على Waseet QR، تُرسل حقول الشحنة إلى `edit-order` أولاً. إذا رفض الوسيط التعديل يظهر السبب. إذا حدث Timeout يجعل نتيجة التعديل غير مؤكدة، يُمنع تعديل جديد حتى مزامنة الشحنة وحسم النسخة الفعلية.

## المزامنة والطباعة

- المزامنة الجماعية تقسم الطلبات إلى دفعات لا تتجاوز 25 معرفاً.
- توجد مزامنة يدوية لطلب واحد ومزامنة جماعية للطلبات المختارة.
- رابط QR المخزن منزوع منه Token.
- PDF يمر عبر Route خادمي يتحقق من نطاق الوسيط ويضيف Token حديثاً؛ لا يرى المتصفح سر حساب التاجر.

## AI Parser

المحلل يقرأ الرسائل العراقية، يوحّد الأرقام والحروف العربية، يستخرج الاسم والهواتف والعنوان والدالة والمبلغ والقطع، ويطابق City/Region مع Metadata الرسمية. المطابقة ذات الثقة المنخفضة تبقى للمراجعة ولا تُرسل تلقائياً. نفس قيم الـSearchable Select هي مصدر الحقيقة للنموذج ولنتيجة التحليل.

## الطلبات القديمة

الكويري يحفظ Snapshot تاريخياً قبل حذف أعمدة الشحن القديمة. الحقول التي قد تحمل Token أو Raw Waseet حساسة تُحجب من الـSnapshot. أسماء المحافظة والمنطقة القديمة يمكن الاحتفاظ بها للمساعدة في المراجعة، لكن لا تتحول إلى IDs رسمية بصمت.

## اختبار القبول قبل الإنتاج

- الدخول والخروج وصلاحيات المدير/المندوب.
- تحديث Cities/Regions/Package Sizes/Statuses.
- Searchable City وRegion المرتبطة بها.
- إنشاء الطلب وحجز كود واحد فقط.
- إعادة نفس الحفظ بنفس رقم الوصل.
- إرسال طلب واحد والحصول على QR.
- انقطاع/Timeout وعدم إعادة الشحنة تلقائياً.
- حسم `needs_verification`.
- تعديل شحنة مسموح بها ومزامنة النتيجة.
- مزامنة دفعة من 25 طلباً أو أقل.
- عرض Driver Notes والحالة الجديدة.
- طباعة PDF من Proxy.
- عرض الطلبات على الحاسوب والتابلت والهاتف.
- مراجعة الطلبات القديمة وعدم إرسالها تلقائياً.

## الاسترجاع

لأن الترحيل يحذف أعمدة Legacy بعد حفظ Snapshot داخل الجدول، الاسترجاع الآمن للإصدار السابق يكون باستعادة نسخة قاعدة البيانات والمشروع المأخوذتين قبل التطبيق، لا بتشغيل كويري عكسي غير مضمون.
